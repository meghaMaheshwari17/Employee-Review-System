<!-- this is navbar -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/employee/dashboard">Employee Review System</a>
      <div class="collapse navbar-collapse justify-content-around" id="navbarNavAltMarkup">
        <% if(locals.user && locals.user.role == 'admin'){ %> 
        <div class="navbar-nav justify-content-around">
          <a class="nav-link mx-5" aria-current="page" href="/dashboard/<%= locals.user._id %>">Home</a>
          <a class="nav-link mx-5" href="/dashboard/<%= locals.user._id %>/admin/addEmployee">Add Employees</a>
          <a class="nav-link mx-5" href="/dashboard/<%= locals.user._id %>/admin/performanceReviews">Performance Reviews</a>
        </div>
        <a class="btn btn-outline-success mx-2" href="/logout">Log Out</a>
        <% } else if (locals.user && locals.user.role == 'employee'){ %>
            <div class="navbar-nav justify-content-around">
                <a class="nav-link mx-5" aria-current="page" href="/dashboard/<%= locals.user._id %>">Home</a>
                <a class="nav-link mx-5" href="/dashboard/<%= locals.user._id %>/employee/feedbacks">Feedbacks by Others</a>
              </div>
              <a class="btn btn-outline-success mx-2" href="/logout">Log Out</a>
        <% } %>
      </div>
    </div>
  </nav>